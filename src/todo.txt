To-Do FEP post-2.4 
(Stand Silvester 2019) 


1) Add bootstrap DM version using SB.gfn
2) Add block bootstrap to both MZ and HP tests using SB.gfn
3) Erläuterung bzw. Referenz generell für den Bootstrap bei MZ und HP (vgl. Rubaskez-Feedback)

4) Prüfe Pesaran&Timmermann 2006, was wohl im Printout erscheint, aber nicht (im Doc) referenziert ist.
(Rubaskez schlägt Verweis vor auf: Pesaran, M. H., & Timmermann, A. (2009). Testing dependence among serially correlated multicategory variables. Journal of the American Statistical Association, 104(485), 325-337)

5) DM: Für fhor>1 wird (hoffentlich) t_HLN berechnet, aber laut unserer Tab. 16 wird nur der entsprechende p-Wert bereitgestellt (als DMpvalssc), nicht die korrigierte
Statistik t_HLN selbst. Das sollte ergänzt werden (als "DMstatssc"), und dann wohl
auch im Ergebnisoutput irgendwie ausgedruckt werden. (vgl. Rubaskez-Feedback)  

6) CW: Im Beispiel wird fc und fc(-1) gemischt, daher sollte die Inferenz HAC-robust sein. Bei uns dient dafür b.fhor > 1, was also ins Beispiel eingefügt werden sollte. Außerdem sollte in der Doku (die sowieso noch richtig ergänzt werden muss) erklärt werden, dass b.fhor hier einzig dafür dient. Alternativ könnte man b.fhor evtl. streichen und einfach immer beide Varianten berechnen, standard und robust. 

### marginales Zeug ####

M1) typo: degress -> degrees